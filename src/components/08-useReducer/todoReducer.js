export const todoReducer = (state = [], action) => {
    switch (action.type) {
        // case 'add':

        //     break;

        //retorna el valor inicial
        default:
            return state
    }
}